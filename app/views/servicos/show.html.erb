<!DOCTYPE html>
<html>
  <head>
    <!-- Incluir a biblioteca jQuery -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <!-- Carregar o arquivo JavaScript para alternância de tema -->
  </head>
  <body>
    <% if notice.present? %>
      <p style="color: green"><%= notice %></p>
    <% end %>

    <%= render partial: 'servicos/servico', locals: { servico: @servico } %>

    <div>
      <%= link_to "Edit this servico", edit_servico_path(@servico) %> |
      <%= link_to "Back to servicos", servicos_path %>

      <%= button_to "Destroy this servico", @servico, method: :delete %>
    </div>
  </body>
</html>
<br>
